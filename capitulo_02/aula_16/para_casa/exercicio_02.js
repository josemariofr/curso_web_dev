// 2. Crie uma função em JavaScript chamada "calcularMedia" que recebe dois parâmetros numéricos, 
// "nota1" e "nota2", e retorna a média aritmética das duas notas.

function calcularMedia(nota1, nota2) {
    return (nota1 + nota2) / 2
}

console.log(calcularMedia(3,8))