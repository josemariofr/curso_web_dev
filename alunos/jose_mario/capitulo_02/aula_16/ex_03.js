// 3. Crie uma função em JavaScript chamada "maiorNumero" que recebe dois parâmetros numéricos,
// "a" e "b", e retorna o maior número entre eles.

function maiorNumero(a, b) {
    if(a > b) {
        return a
    } return b
}
console.log(maiorNumero(1, 0))
 